import Vue from 'vue'
import Router from 'vue-router'
import HelloWorld from '@/components/HelloWorld'
// 路由的配置和对象在此文件夹内
Vue.use(Router)

export default new Router({
  routes: [
    {
      path: '/',
    }
  ]
})
